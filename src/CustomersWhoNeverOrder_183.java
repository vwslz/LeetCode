public class CustomersWhoNeverOrder_183 {
    /**
     * mysql
     * LEFT JOIN: for the columns which has no coresponding row in LEFT JOIN table, the value will be NULL
     */
//    SELECT Name AS Customers
//    FROM Customers
//    LEFT JOIN Orders
//    ON Customers.Id = Orders.CustomerId
//    WHERE Orders.CustomerId IS NULL;
}
